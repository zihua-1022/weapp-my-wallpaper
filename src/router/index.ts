// import pagesConfig from "./pages.config";
// import tabBarConfig from "./tabBar.config";
const pagesConfig = require("./pages.config");
const tabBarConfig = require("./tabBar.config");

const routerConfig = { pagesConfig, tabBarConfig };

module.exports = routerConfig;
